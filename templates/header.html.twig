
<div class="row mx-0 px-0">
    <div class="d-flex align-items-center justify-content-around justify-content-sm-between">
        <img src="{{ asset("build/images/coq_new.svg") }}"  alt="corporate identity rooster logo" class="rounded-circle m-3 col-3 col-sm-2 col-xl-1">
        <h1 class="text-uppercase text-center mt-3 col-6 col-sm-4 main-color">Agribourg</h1>
      <a href="{{ path('app_shopping_basket') }}">
            <img src="{{ asset("build/images/cart.png") }}" alt="" class="col-3 col-sm-1 d-none d-sm-block img-cart">
        </a>
    </div>
</div>
<nav class="navbar navbar-expand-lg mt-2 background-main-color">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav justify-content-start">
                <li class="nav-item">
                    <a class="nav-link text-color-card-products" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-color-card-products" href="{{ path('app_company_history') }}">Notre histoire</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-color-card-products" href="{{ path('app_search_products') }}">Produits</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-color-card-products" href="{{ path('app_expertise') }}">Savoir-faire</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-color-card-products" href="#">Témoignages</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-color-card-products" href="#">Contacts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-color-card-products d-block d-sm-none" href="#">Panier</a>
                </li>
            </ul>
            <ul class="navbar-nav justify-content-end">
                <li class="nav-item">
                    {%  if app.user %}
                        <a class="text-color-card-products nav-link">Bienvenue {{ app.user.prenom }}</a>
                    {%  endif %}
                </li>
                <li class="nav-item">
                    {%  if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('dashboard') }}" class="text-color-card-products nav-link">Dashboard</a>
                    {%  endif %}
                </li>
                <li class="nav-item">
                    {% if is_granted('ROLE_USER') %}
{#                        <a class="nav-link text-color-card-products" href="{{ path('profile_user') }}">Profil</a>#}
                        <a class="nav-link text-color-card-products" href="{{ path('profile_user_show', {'id': app.user.id}) }}">Show Profil</a>
                    {%  else %}
                        <a class="nav-link text-color-card-products" href="{{ path('app_register') }}">Inscription</a>
                    {%  endif %}
                </li>
                <li class="nav-item">
                    {%  if app.user %}
                        <a class="nav-link text-color-card-products" href="{{ path('app_logout') }}">Déconnexion</a>
                    {%  else %}
                        <a class="nav-link text-color-card-products" href="{{ path('app_login') }}">Connexion</a>
                    {%  endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>




